import Blog from "../../../Shared/Blog";

const TabsReact = () => {
    return (
        <div>
            <Blog
            topic={'Tabs'}
            npmCode={'npm install --save react-tabs'}
            url={'https://www.npmjs.com/package/react-tabs'}
            />
        </div>
    );
};

export default TabsReact;